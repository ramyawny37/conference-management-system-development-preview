(function(global){
  'use strict';
  var rootId='current_device_authorization_root',state={status:'unavailable',accountStatus:null,enforcement:null,message:''},generation=0,sessionUserId='',authSubscription=null,initializationPromise=null;
  var labels={not_registered:'غير مسجل',registered:'مسجل',pending:'بانتظار الموافقة',approved:'معتمد',revoked:'مرفوض أو ملغى',unavailable:'تعذر التحقق'};
  function service(){return global.CurrentDeviceAuthorizationService;}
  function root(){return global.document&&global.document.getElementById(rootId);}
  function escapeHtml(value){return String(value==null?'':value).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
  function failureMessage(result,prefix){var error=result&&result.error||{},code=String(error.code||result&&result.status||'UNKNOWN_ERROR'),message=String(error.message||'');return String(prefix||'تعذر تنفيذ العملية.')+' ['+code+']'+(message?' '+message:'');}
  function paint(){var el=root(),identity=global.SupabaseDeviceIdentity&&global.SupabaseDeviceIdentity.getOrCreate?global.SupabaseDeviceIdentity.getOrCreate():null;if(!el)return;if(state.status==='approved'){if(el.style)el.style.display='none';el.innerHTML='';return;}if(el.style)el.style.display='';var status=labels[state.status]||labels.unavailable,action='',enforcement=state.enforcement===true?'تطبيق اعتماد الأجهزة مفعّل.':state.enforcement===false?'تطبيق اعتماد الأجهزة غير مفعّل حاليًا.':'تعذر التحقق',name=escapeHtml(identity&&identity.deviceName||identity&&identity.platform||'—'),id=escapeHtml(identity&&identity.id||'—');if(['not_registered','registered','pending'].indexOf(state.status)>=0)action='<button type="button" class="btn btn-green btn-sm" onclick="CurrentDeviceAuthorizationUI.requestApproval()">طلب الاعتماد</button>';else if(state.status==='revoked'&&state.accountStatus==='approved')action='<button type="button" class="btn btn-green btn-sm" onclick="CurrentDeviceAuthorizationUI.requestApproval()">طلب اعتماد الجهاز مرة أخرى</button>';el.innerHTML='<section class="settings-section sync-settings-section"><div class="settings-section-title">حالة اعتماد هذا الجهاز</div><p><strong>اسم الجهاز الحالي:</strong> '+name+'</p><p dir="ltr"><strong>Device ID:</strong> '+id+'</p><p><strong>حالة الجهاز:</strong> '+status+'</p><p>'+state.message+'</p><p>'+enforcement+'</p><div class="sync-settings-actions">'+action+'<button type="button" class="btn btn-gray btn-sm" onclick="CurrentDeviceAuthorizationUI.refresh()">تحديث الحالة</button></div></section>';}
  function refresh(){var token=++generation;return Promise.all([service().getStatus(),service().getDeviceAwareAccess()]).then(function(values){if(token!==generation)return {ok:false,status:'stale'};var status=values[0],access=values[1],data=status&&status.data||{},aware=access&&access.data||{};if(status&&status.ok)state.status=String(data.deviceAuthorizationStatus||'unavailable');state.accountStatus=String(aware.accountStatus||state.accountStatus||'');state.enforcement=typeof aware.enforcementEnabled==='boolean'?aware.enforcementEnabled:state.enforcement;if(!status||!status.ok)state.message=failureMessage(status,'تعذر تحديث حالة الجهاز.');else if(aware.accountStatus&&aware.accountStatus!=='approved')state.message=aware.accountStatus==='blocked'?'الحساب محظور إداريًا. راجع المسؤول.':'حساب المستخدم غير معتمد للوصول إلى النظام.';else if(state.status==='approved')state.message='هذا الجهاز معتمد ويمكنه استخدام الخدمات المحمية.';else if(state.status==='pending')state.message='هذا الجهاز بانتظار موافقة مدير المؤسسة.';else if(state.status==='revoked')state.message='تم رفض أو إلغاء اعتماد هذا الجهاز. يمكنك طلب اعتماده مرة أخرى.';else if(state.status==='registered')state.message='هذا الجهاز مسجل. أرسل طلب اعتماد صريح.';else if(state.status==='not_registered')state.message='يجب تسجيل هذا الجهاز ثم إرسال طلب اعتماد صريح.';else state.message='لن يتم اعتماد الجهاز تلقائيًا.';paint();if(global.DeviceReauthorizationFlow&&typeof global.DeviceReauthorizationFlow.handleAuthorizationState==='function')global.DeviceReauthorizationFlow.handleAuthorizationState(state.status);return status;});}
  function registerDevice(){return service().registerCurrentDevice().then(function(result){if(!result.ok){state.message=failureMessage(result,'تعذر تسجيل الجهاز.');paint();return result;}return refresh().then(function(){return result;});});}
  function requestAuthorization(){if(global.confirm&&!global.confirm('هل تريد إرسال طلب اعتماد لهذا الجهاز؟'))return Promise.resolve({ok:false,status:'cancelled'});return service().requestAuthorization().then(function(result){if(!result.ok){state.message=failureMessage(result,result.status==='ambiguous'?'نتيجة الطلب غير مؤكدة. حدّث الحالة قبل إنشاء طلب جديد.':'تعذر إرسال طلب الاعتماد.');paint();return result;}return refresh().then(function(){return result;});});}
  function requestApproval(){if(state.status==='not_registered')return registerDevice().then(function(result){return result&&result.ok?requestAuthorization():result;});return requestAuthorization();}
  function ensurePendingAuthorization(){
    return service().registerCurrentDevice().then(function(registered){
      if(!registered||!registered.ok)return registered;
      return refresh().then(function(){
        if(state.status==='approved'||state.status==='pending'){
          return {ok:true,status:state.status};
        }
        if(['registered','revoked'].indexOf(state.status)<0){
          return {ok:false,status:state.status||'unavailable'};
        }
        return service().requestAuthorization().then(function(requested){
          return refresh().then(function(){return requested;});
        });
      });
    });
  }
  function bindAuth(){var session=global.SupabaseAuth&&global.SupabaseAuth.getSession&&global.SupabaseAuth.getSession(),client=global.SupabaseClientLayer&&global.SupabaseClientLayer.getClient&&global.SupabaseClientLayer.getClient();sessionUserId=session&&session.user&&String(session.user.id||'')||'';if(authSubscription||!client||!client.auth||typeof client.auth.onAuthStateChange!=='function')return;var listener=client.auth.onAuthStateChange(function(event,nextSession){var next=nextSession&&nextSession.user&&String(nextSession.user.id||'')||'';if(next===sessionUserId)return;sessionUserId=next;generation++;state={status:'unavailable',accountStatus:null,enforcement:null,message:'لن يتم اعتماد الجهاز تلقائيًا.'};paint();if(next)refresh();});authSubscription=listener&&listener.data&&listener.data.subscription||null;}
  function initialize(){if(initializationPromise)return initializationPromise;paint();var auth=global.SupabaseAuth&&global.SupabaseAuth.initialize?global.SupabaseAuth.initialize():Promise.resolve();initializationPromise=Promise.resolve(auth).then(function(){bindAuth();return refresh();});return initializationPromise;}
  function getState(){return JSON.parse(JSON.stringify(state));}
  global.CurrentDeviceAuthorizationUI=Object.freeze({initialize:initialize,refresh:refresh,registerDevice:registerDevice,requestAuthorization:requestAuthorization,requestApproval:requestApproval,ensurePendingAuthorization:ensurePendingAuthorization,getState:getState});
})(window);
